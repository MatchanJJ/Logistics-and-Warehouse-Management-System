<a href="/">
    <img class="home" src="/home-button.png" alt="home">
</a>
<h2>Employee List</h2>
<a href="/add-employee" class="btn">Add New Employee</a>
<a href="/manage-job-roles" class="btn">Manage Job Role</a>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Contact Info</th>
            <th>Role ID</th>
            <th>Salary</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <% employees.forEach(employee => { %>
        <tr onclick="window.location.href='/employee/<%= employee.employee_id %>'" style="cursor: pointer;">
            <td><%= employee.employee_id %></td>
            <td><%= employee.employee_first_name %></td>
            <td><%= employee.employee_last_name %></td>
            <td><%= employee.contact_info %></td>
            <td><%= employee.employee_role_id %></td>
            <td><%= employee.employee_salary %></td>
            <td>
                <a href="/update-employee/<%= employee.employee_id %>"><button>Update</button></a> 
                <form action="/delete-employee/<%= employee.employee_id %>" method="POST" style="display:inline;">
                    <button type="submit">Delete</button>
                </form>
            </td>
        </tr>
        <% }) %>
    </tbody>
</table>
